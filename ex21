#include <stdio.h>

void printExclamation(int n);

void main()
{
    int n;

    scanf("%d", &n);
    printExclamation(n);
}

void printExclamation(int n)
{
    int i, j;

    for (i = 1; i <= n; i++)
    {
        for (j = 1; j <= i; j++)
            printf("!");
        printf("\n");
    }
}
