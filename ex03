#include <stdio.h>

int checkNumber(int number);

void main()
{
    int number;

    scanf("%d", &number);
    printf("%d\n", checkNumber(number));
}

int checkNumber(int number)
{
    if (number > 0)
        return 1;
    else if (number < 0)
        return -1;
    else
        return 0;
}
